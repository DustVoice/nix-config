{
  dustvoice.fish.homeManager = {
    programs.fzf = {
      enable = true;
      enableFishIntegration = true;
    };

    programs.fish = {
      enable = true;

      plugins = [ ];
    };
  };
}
